# M3 拼音 IME 实施记录（2026-07-11）

## 架构

```
aime-ime.app（IMKit 进程，~/Library/Input Methods/）
└── AimeInputController：按键状态机 + 组合区 + IMKCandidates 候选窗
    └── AimePinyin（共享库，无重依赖）
        ├── PinyinTable        无声调音节全表（ü→v 习惯拼写）
        ├── PinyinSegmenter    DP 切分 lattice + 中英混输透传 + 键盘错误修复
        ├── FuzzyRules         11 组模糊音（可配置，默认开南方六组）
        ├── PinyinConverter    结构化 prompt + OpenAI 兼容整句转换
        ├── UserDictionary     用户词库 v1（JSON，频次×新近度）
        └── SharedConfig       app ↔ IME 进程配置共享（UserDefaults suite）
```

与 ds-input 的核心差异：拼音不裸扔 LLM，规则层先做切分/纠错/标注，
LLM 拿到的是结构化分析（`hao(疑似手误,实际敲的是hso)`、`[原样保留:API]`、模糊音候选），
且 prompt 硬约束"逐音节转写、禁增删字"。

## 键位约定

| 键 | 行为 |
|---|---|
| a–z | 进组合区，180ms 防抖触发 LLM 整句转换 |
| 大写/数字/`'` | 进组合区（混输透传/显式分隔） |
| 空格 | 上屏首选（转换未回来则待命，回来即上屏） |
| 回车 | 上屏原始拼音 |
| 数字 1–9 | 候选窗选字（首选/句级备选/原始拼音） |
| Esc | 取消组合 |
| `,` `.` `?` 等 | 上屏当前最优 + 中文标点 |

组合区样式：转换预览=实线下划线；未转换拼音=点线下划线。
上下文：经 IMKTextInput 直接读宿主应用光标前 120 字（无需辅助功能权限）。

## 验收结果

- **单元测试 12/12 绿**（`swift test`）：切分/歧义/混输/修复/模糊音全覆盖
- **`nihsoshijie`**（a→s 手误）：切分修复 ✅ → LLM 输出「你好世界」✅
- **`zheshiyigeAPIjiekou`**：切分 `P:zhe-shi-yi-ge | L:API | P:jie-kou` ✅ → 「这是一个API接口」✅
- **20 句含错测试集首选准确率 85–90%**（`aime-pinyin --suite testdata/pinyin_testset.tsv`，
  DeepSeek，p50 延迟 0.55s）。错误集中在语义歧义句与 LLM 偶发的意译倾向
- IME 安装链路：`make install-ime` → TIS 注册+启用 ✅（已在系统输入源列表）

## 已知局限 / 顺延项

- **双拼**：未做（M3.5：键位映射表在切分前转换即可，与纠错的交互需要设计）
- **实机打字体验未走查**：候选窗行为、组合区在各应用（浏览器/终端/Electron）的兼容性
  需要真实使用反馈；IMKCandidates 的数字选字与翻页可能需要调
- 整句延迟依赖 API（p50 0.55s）：M5 本地小模型是根本解
- prompt 意译倾向未根除（如 xiazhouyiyao→"下周就要"）：候选 few-shot 加强或换更听话的模型
- Tab 段间跳转（逐段消歧）留 M4，与语音入 composition 一起做

## 调试工具

- `swift run aime-pinyin <拼音>`：看切分 + LLM 转换
- `aime-pinyin --suite testdata/pinyin_testset.tsv`：准确率回归
- 设置 → 拼音 → Playground：图形化调试
- IME 日志：`log stream --predicate 'process == "aime-ime"'`

# aime 拼音输入法：算法与流程详解

> 面向没有输入法/NLP 背景的读者。每节先讲"要解决什么问题"，再讲"怎么解决"。
> 状态标注（2026-07-11 更新）：全部四层均已实现。评测（40 句含错集）：首选 85%，句级覆盖 92%，本地整句 62%。

## 0. 拼音输入为什么难

把 `jintianxiawukaihui` 变成"今天下午开会"，机器要跨过四道坎：

1. **没有空格**。用户敲的是一串连续字母，机器先要猜出音节边界：是 `jin|tian` 还是
   `ji|ntian`？有些串天然歧义：`xian` 既是一个音节（先/现），也可以是 `xi|an`（西安）。
2. **同音字爆炸**。一个音节平均对应几十个汉字，`shi` 有上百个。六个音节的句子，
   理论组合是天文数字，必须靠"哪个组合像人话"来选。
3. **输入是脏的**。手误（`hao` 敲成 `hso`）、口音（分不清 zhi/zi）、漏字母、多字母、
   打到一半的音节——真实输入充满噪声，输入法必须"猜你想打什么"而不是"按你打的转换"。
4. **中英混杂**。`zheshiyigeAPIjiekou` 里的 `API` 不是拼音，要原样保留。

传统输入法（搜狗、RIME）靠"词库 + 统计模型"解决 2；aime 的差异是引入 LLM 做整句
语义决策，但**不把脏活全推给 LLM**——切分、纠错、枚举候选这些结构化工作由规则层和
词库层完成，LLM 只做它最擅长的：结合上下文选出最像人话的整句。

## 1. 全景：一次按键发生了什么

```
用户敲一个字母
    │
    ▼
┌─ 第一层：切分 + 拼写变换（微秒级，纯规则）──────────【已实现】
│   字母串 → 音节序列（每个音节带"可信度"和备选）
│   "nihsoshijie" → [ni][hao(手误修复)][shi][jie]
│
├─ 第二层：词库 + 本地造句（微秒级，查表+动态规划）───【已实现】
│   音节序列 → 词候选 + 一个"本地整句"
│   [jin][tian][xia][wu] → 候选:今天/金/… 整句:"今天下午"
│   ← 这一步让候选栏在按键瞬间就有内容，不用等网络
│
├─ 第三层：LLM 整句转换（约 0.6 秒，网络请求，180ms 防抖）─【已实现】
│   把切分分析 + 上下文发给大模型 → 语义最优的整句
│
├─ 第四层：回验（毫秒级，本地）────────────────────【已实现】
│   把 LLM 返回的句子逐字转回拼音，和用户输入比对，
│   读音对不上的直接丢弃 ← LLM 偶尔瞎编，这里兜底
│
▼
候选栏 = [LLM整句] [本地整句] [词候选…] [原始拼音]
组合区预览实时更新；空格上屏首选，数字选其他候选
```

设计哲学一句话：**规则层管"音对不对"，词库层管"是不是词"，LLM 管"像不像人话"，
回验管"LLM 有没有撒谎"**。每层只做自己擅长的事。

## 2. 第一层：切分——把字母串断成音节

### 2.1 音节表

普通话的全部合法音节（不带声调）只有约 410 个：`a、ai、an、…、zhuang、zi、zong…`。
这是一张写死的表。任何切分方案的每一段都必须在表里，这是最基本的约束。

### 2.2 切分为什么要"找最优"而不是"找一个"

`xianzai` 至少有两种合法切法：`xian|zai`（现在）和 `xi|an|zai`（西安在）。
机器无法确定哪个是用户想要的，所以策略是：

- **选一个最可能的**作为主解（规则：音节越少越好，因为人打字倾向连贯）；
- **歧义信息不丢**，作为备选传给后面的层（LLM 的 prompt 里会带上）。

"从一堆合法切法里挑总代价最小的"是一个经典算法问题，解法叫**动态规划**（DP）。

### 2.3 动态规划三分钟入门

以 `nihao` 为例。给每个字母间隙编号：

```
 n  i  h  a  o
0  1  2  3  4  5      ← 位置 0 是开头，位置 5 是结尾
```

问题变成：**从位置 0 走到位置 5，每一步必须跨过一个合法音节，怎么走总代价最小？**
每跨一个音节记 1 分代价（音节越少总分越低）：

```
从 0 出发能走到哪？
  0→2 (ni ✓)   0→1 (n ✗ 不是音节)
从 2 出发？
  2→5 (hao ✓)  2→4 (ha ✓)  2→3 (h ✗)
从 4 出发？
  4→5 (o ✓)

所有完整走法：
  0→2→5:  ni|hao      代价 2   ← 最优
  0→2→4→5: ni|ha|o    代价 3
```

DP 的聪明之处在于不枚举所有走法：每个位置只记住"走到这里的最优解"，
后面的计算直接复用。位置再多也是线性时间。
**这个"图上找最优路径"的算法就是 Viterbi**——后面词库层还会用它一次。

### 2.4 中英混输

大写字母、数字一律"原样保留"（`API`、`3`），不参与拼音解析——它们进入切分结果里
的 literal 段。小写但无法解析成拼音的串（比如 `vvv`），最后才退化成原样保留，
代价设得很高，保证"能当拼音解释就当拼音解释"。

## 3. 拼写变换：把脏输入洗干净

### 3.1 五种错误，统一建模

真实输入的错误就那么几类。aime 把它们统一成"拼写变换"：每种变换给一个
**可信度**（这个解释有多可能是真相），可信度换算成 DP 里的代价——
可信度越低，代价越高，只有当"干净解释"不存在时才启用：

| 变换 | 例子 | 可信度 | 原理 |
|---|---|---|---|
| 原样合法 | `hao` | 1.0 | 音节表直查 |
| 模糊音 | `zi` ↔ `zhi` | 0.9 | 口音规则表（见 3.2） |
| 临近键替换 | `hso`→`hao` | 0.7 | s 和 a 在键盘上相邻，手滑 |
| 相邻换位 | `mne`→`men` | 0.65 | 两根手指落键顺序颠倒 |
| 漏敲字母 | `zhng`→`zhang/zheng/zhong` | 0.6 | 见 3.3 |
| 多敲字母 | `zhoong`→`zhong` | 0.6 | 逐位删一个字母试 |
| 句尾没打完 | `nihaosh` 的 `sh` | 0.8 | 见 3.4 |

这个"统一可信度"的思路借鉴自 RIME 的拼写运算（spelling algebra）。好处：
排序有统一标尺，LLM 的 prompt 标注有统一措辞，以后加新错误类型不用改架构。

### 3.2 模糊音

方言口音的系统性混淆：平翘舌（z/zh、c/ch、s/sh）、鼻边音（n/l）、前后鼻音
（in/ing、en/eng、an/ang）等 11 组，用户按自己的口音在设置里勾选。
模糊音不改变切分边界，而是给音节挂"备选读音"：`zi` 挂上 `zhi`，
后面的词库层和 LLM 都会同时考虑两种读音。

### 3.3 漏敲的反查表技巧

"用户漏敲了一个字母"意味着：他敲出的串 = 某个完整音节挖掉一个字母。
所以启动时预先把每个音节的每个"挖字变体"都生成出来，做成反查表：

```
zhang 挖字 → hang? zang? zhng? zhag? zhan?…
其中 zhng 不是合法音节 → 登记 deletionMap["zhng"] += zhang
（zheng、zhong 挖字也得到 zhng）→ deletionMap["zhng"] = [zhang, zheng, zhong]
```

运行时用户敲了 `zhng`，O(1) 查表就知道"他可能想打 zhang/zheng/zhong"。
约 2500 条，内存可忽略。

### 3.4 句尾 partial：实时体验的关键细节

打字是渐进的。输入到 `nihaosh` 时，`sh` 不是错误——是 `shi/shuo/shang…` 打了一半。
如果把它当成"无法解析"扔掉，实时预览就会一闪一闪地出垃圾。
所以：**仅当破碎串出现在末尾**时，若它是某些音节的前缀，标记为"未打完"，
并把可补全的音节列表告诉 LLM（"最后一个音节可能是 shi/sha/shu…"）。

## 4. 第二层：词库与本地造句【已实现】

### 4.1 为什么需要这层（而不是全靠 LLM）

三个原因：
- **延迟**：LLM 要 0.6 秒，打字的手感需要毫秒级反馈；
- **候选多样性**：LLM 擅长给一个最优解，不擅长穷举"所有说得通的选项"；
  词库天然是穷举——每个词库词都是真实存在的词，语义合理性由构造保证；
- **兜底**：断网、LLM 抽风时，输入法仍然要能打字。

### 4.2 词库的样子

约 5 万条高频词（来自 jieba 开源词频表，MIT 许可），每条 =（音节序列，词，频次）：

```
jin tian   今天  825000
jin        金    310000
xia wu     下午  259000
```

按音节序列排序存储。**排序数组本身就是查询结构**：找"以 jin tian 开头的词"
= 二分查找定位区间，区间内全是匹配（今天、今天下午、今天晚上…）。不需要建树。

注音管线：jieba 只有词没有拼音，用 macOS 自带的汉字→拼音转换逐词生成，
多音字（银行/音乐/重庆）靠人工补丁表修。注错的词只是"查不到"（伤召回），
不会产生错误候选（不伤精度）——失败方向是安全的。

### 4.3 本地造句：第二次 Viterbi

切分给出音节序列后，"怎么组词最像话"又是一个找最优路径问题——
节点换成音节位置，边换成词库里的词，得分换成词频：

```
音节:   jin   tian   xia   wu    kai   hui
位置:  0     1      2     3     4     5     6

边（词）:  0→2 今天(高频)  0→1 金  1→2 天  2→4 下午(高频)
          2→3 下  3→4 午  4→6 开会  4→5 开  5→6 会

得分 = Σ log(词频) − λ × 词数
```

两个细节为什么这样设计：
- **取 log**：词频从几百到几百万跨五个数量级，直接相加会被超高频词绑架；
  取对数后"高频词好一点，但好得有限"，符合直觉。
- **λ（每词罚分）**：不罚的话，一堆高频单字（金+天+下+午）可能干过整词，
  因为单字个个都高频。λ 相当于"说话尽量用整词"的先验。λ 是这层唯一的
  重要超参数，用评测集调，不拍脑脑袋。

跑一遍 DP（和切分那次算法完全一样），得到：
- **一个本地整句**（"今天下午开会"）——按键瞬间就能显示的底稿；
- **每个位置的词候选表**（今天/金/今…）——候选栏的主体。

### 4.4 与拼写变换的配合

第一层给出的音节带可信度和备选，词库查询时一起考虑，可信度乘进得分：

```
输入 zhngguo（漏敲）:
  假设 zhong(0.6)+guo(1.0) → 查到"中国"(高频) → 得分 = log(频) + log(0.6)
  假设 zheng(0.6)+guo(1.0) → 查到"正果"(低频) → 得分低得多
→ 本地层就能选出"中国"，不用等 LLM
```

大量纠错在这一层毫秒级解决，LLM 只处理剩下的语义难题。

### 4.5 诚实的能力边界

本地造句只有词频（术语叫 unigram），没有"词和词搭不搭"的知识（bigram/语法）。实测本地整句命中 62%。
经典失败：`yanjiushengmingkexue` → "研究生|命|科学" vs "研究|生命|科学"，
两条路的词频总分几乎一样，本地层掷硬币。**这类歧义正是留给 LLM 的**——
0.6 秒后 LLM 的结果会接管首位。本地句的定位是即时底稿，不是质量担当。

## 5. 第三层：LLM 整句转换【已实现】

### 5.1 为什么不把原始字母串直接扔给 LLM

对比实验（M3 调试期的真实案例）：裸扔 `zheshiyigelanren`，LLM 输出过"格兰人"
（音全对但不是词）；裸扔时它也不知道 `nihso` 里藏着手误。
**LLM 缺的不是语言能力，是对输入噪声结构的了解**——这正是前两层挖出来的信息。

### 5.2 结构化 prompt

发给 LLM 的不是字母串，是分析报告：

```
原始按键：nihsoshijie
切分分析：ni(也可能是:li) hao(疑似手误,实际敲的是hso) shi(也可能是:si) jie
光标前文本：（从宿主应用读到的上文）
用户常用词：aime、IMKit、…（用户词库 top-N）
```

配上硬约束规则：**逐音节转写**（每个音节对应恰好一个汉字，禁止增删字、换近义词——
否则 LLM 会把"这个"润色成"这份"）、英文段原样保留、备选宁缺毋滥。

### 5.3 上下文从哪来

输入法进程能直接从宿主应用读光标前的文本（IMKit 提供的能力，不需要额外权限）。
"在代码评审工具里打 zhegepr" 和 "在外卖软件里打 zhegepr"，LLM 拿到的上文不同，
输出就不同——这是传统输入法没有的能力。

## 6. 第四层：回验——不让 LLM 撒谎【已实现】

LLM 偶尔会输出读音对不上的字（幻觉）。防线：把它返回的句子**逐字转回拼音**
（用系统自带的转换，零数据依赖），和用户输入的音节假设集比对：

```
用户输入: zhe shi yi ge lan ren（lan 挂着模糊备选 nan）
LLM 返回: 这是一个男人 → zhe shi yi ge nan ren → nan 命中模糊备选 ✓ 通过
LLM 返回: 这是一个船员 → chuan yuan 对不上 lan ren ✗ 丢弃
```

多音字（行/长/乐）用小补丁表兜底；一个字对不上降权，两个字以上直接丢。
**回验不通过时，本地整句顶上首位**——LLM 永远没有资格用错误答案阻塞用户。

## 7. 候选栏与上屏交互

### 7.1 时间线

```
t=0ms    按键 → 本地层同步完成
         候选栏: │1.今天下午开会(本地)│2.今天│3.金│…│9.原始拼音│
         组合区: 今天下午开会（点线下划线 = 未定稿）
t≈600ms  LLM 返回且过回验
         候选栏: │1.LLM整句│2.本地整句(若不同)│3.今天│…│
         组合区: LLM整句（实线下划线 = 高置信）
```

用户随时可以按空格（拿首选）、按数字（拿其他候选）、按回车（拿原始拼音）——
不需要等 LLM。快打字的人感知不到 0.6 秒，因为底稿一直在。

### 7.2 逐段确认（部分上屏）

整句不对时，用户选一个**词**候选（比如"今天"）：

```
确认"今天" → 消耗对应的按键（jintian）→ 剩余 xiawukaihui 重新走全流程
组合区: 今天‸下午开会    ← 已确认部分固定，活动段继续转换
```

这个"已确认前缀 + 活动段"的模型借鉴 RIME。退格先删活动段字母，
活动段空了再整词回退上一次确认。这保证**永远有路可走**：
最坏情况下逐词把整句拼出来，每一步的选项都来自真实词库。

### 7.3 学习闭环

上屏即学习：确认过的词进用户词库（带时间衰减，旧习惯自然淡出），
下次注入 LLM prompt 且在词候选里加权置顶。语音输入侧共享同一个词库
（拼音教过的专有名词，语音识别也认识）——这是 aime "双模态共享大脑"的一角。

## 8. 各层延迟与体积预算

| 层 | 延迟 | 常驻内存 |
|---|---|---|
| 切分 + 拼写变换 | <1ms | ~1MB（反查表/前缀集） |
| 词库查询 + 本地造句 | <1ms | ~10MB（5 万词条） |
| LLM 转换 | ~600ms（防抖 180ms 后发起） | — |
| 回验 | <5ms | 忽略 |

## 9. 术语小抄

- **音节（syllable）**：拼音的最小合法单位，如 jin、tian。全表约 410 个。
- **切分（segmentation）**：把连续字母断成音节序列。
- **lattice（网格）**：一个位置上保留多种假设（音节/读音/词）的数据结构，
  "不过早下结论"的载体。
- **动态规划 / Viterbi**：在分层图里找最优路径的算法，每个节点只记最优前缀解。
  本文档里出现两次：字母→音节、音节→词。
- **unigram / bigram**：只看单个词的频率 / 还看相邻两个词的搭配频率。
- **可信度（credibility）**：某个拼写变换"是真相"的先验概率，参与所有排序。
- **回验（verification）**：把候选汉字转回拼音与输入比对，过滤 LLM 幻觉。

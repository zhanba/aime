# M4 三场景走查清单（需实机验证）

前置：`make install-ime && make bundle && open build/aime.app`；设置 → 高级确认后台服务"已注册"；
切到「aime拼音」。首次在输入法里按住右 Option 会弹 aime-daemon 的麦克风授权。

## 场景一：纯拼音盲打（回归）
- [ ] 打 `jintianxiawukaihui`，按键瞬间有本地预览与候选，空格上屏
- [ ] 故意打错 `nihsoshijie` → 预览/候选正确修复

## 场景二：语音听写 + 回改修正
- [ ] 空组合态按住右 Option 说一句话 → 组合区流式出现点线文本
- [ ] 松开 → 文本定稿（粗下划线确认段）→ 空格上屏
- [ ] 立刻 Shift+Space → 刚上屏的句子回到组合区
- [ ] 退格 → 语音文本变成拼音重解释，候选栏出现同音候选 → 数字选正确的词逐段修

## 场景三：打字 + 按住说话 + 继续打字，一次提交
- [ ] 打 `zhegepr`（不上屏）→ 按住右 Option 说"下午之前要合并"→ 松开
- [ ] 拼音预览自动确认 + 语音段追加（组合区两段粗下划线）
- [ ] 继续打 `wojuedekeyi` → 空格 → 三段一次性上屏

## 场景四：语音消歧（附加）
- [ ] 打 `shiyan`（不上屏）→ 按住右 Option 说"实验"→ 松开 → 预览变为「实验」（实线）
- [ ] 同样输入说一句不相关的话 → 自动判为追加而不是替换

## 隐私验证
- [ ] 设置 → 隐私 → 屏蔽某应用 → 该应用内打拼音只有本地候选（无 AI 角标）
- [ ] 纯本地模式开启 → 任何应用都不再出现 AI 候选

已知边界：flagsChanged 在个别宿主应用可能不可靠（表现为按住 Option 无反应）——
遇到请记录应用名；语音段含英文时退格为整段删除（反推拼音只支持纯中文）。

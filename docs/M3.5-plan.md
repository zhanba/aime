# M3.5 方案 v3：容错强化 + 词库层候选（待评审）

## 对 RIME/Squirrel 的借鉴分析（2026-07 调研）

RIME 的架构与我们的映射关系：音节图（syllable graph）≈ 我们的切分 lattice；
拼写运算（spelling algebra）≈ 模糊音+纠错的统一框架；词典+造句 ≈ 本方案 W2；
它没有的：LLM 整句转换与上下文感知——这正是 aime 的增量。五个可直接借鉴的设计：

1. **拼写变换统一框架**（借鉴拼写运算）：RIME 把 模糊音/缩写/变体/纠错 全部建模为
   "带可信度的拼写变换"，统一喂进音节图，可信度参与排序。我们现在模糊音、键盘纠错、
   （将做的）漏敲多敲是三套散装逻辑——应统一为 `SpellingTransform(变换, 可信度)`：
   精确 1.0 > 模糊音 0.9 > 临近键替换 0.7 > 换位 0.65 > 漏敲/多敲 0.6 >（Phase2 简拼 0.5）。
   可信度同时进候选排序和 LLM prompt 标注。
2. **纠错的可视提示**（借鉴 librime corrector，PR #228：对称删除搜索 + 有限候选 + 惩罚）：
   被修复的音节在组合区里用不同样式标出（我们用下划线颜色/样式区分），用户对"它替我改了字"
   有知情权——信任感的关键细节。
3. **本地即时造句**（借鉴 essay 词频 + octagram 语法的 DP 造句）：RIME 的默认整句是
   纯本地毫秒级出的。借鉴到 W2：词库 trie 上跑 Viterbi（词频 unigram 起步），
   **每次按键即时给出一个本地整句候选**作为默认；LLM 结果 ~0.6s 后到达、过回验后
   接管首位。收益：断网可用、感知延迟为零、LLM 垃圾永远不会阻塞打字。
4. **逐段确认模型**：RIME 的 composition = 已确认前缀 + 活动段候选，选词即确认段并前进。
   我们的"部分上屏"直接采用这个模型（也为 M4 逐段修正打底）。
5. **用户词典 tick 衰减**：RIME 用户词典按使用节拍衰减旧习惯词频。我们的 UserDictionary
   从 count+recency 升级为衰减加权，并借鉴"从整句提交中切词学习"（用词库 trie 切）。

**要不要直接嵌入 librime？**（BSD-3，法律上可行）——结论：**不嵌**。librime 的组合状态机
与我们"LLM 整句为主、异步到达"的流程会互相打架，桥接成本高于自建轻量词库层；
其数据（八股文/明月拼音词库）是 LGPL，嵌进二进制也别扭。取其设计，不取其依赖；
M5 本地化阶段若需要更强本地引擎再重估。Squirrel 的自绘候选窗（非 IMKCandidates）
印证了 W4 的方向（GPL，只学行为不抄代码）。

---

## 设计原则修正（来自评审反馈）

1. **枚举不交给 LLM**。LLM 不擅长穷举读音×词边界的组合空间，强迫它出 N 个候选，
   第 2 个之后就开始凑数——语义垃圾（"格兰人"）正是这么来的。
   LLM 只做它擅长的：结合上下文给 1 个最优整句 +（仅当真有歧义时）1 个语义不同的备选。
2. **"更多选项"来自词库层**。传统 IME 的正解：候选多样性由 拼音 lattice × 真实词库
   确定性枚举产生。词库里的每一项都是真实存在的词——**语义合理性由构造保证**，
   不需要事后过滤。这同时解决"选项少"和"明显不合理的选项"两个问题。

## 候选栏的目标形态

```
输入 zheshiyigelanren，LLM 返回前（即时，<5ms）：
│1. 这│2. 这是│3. 折│4. 蔗│…        ← 词库前缀候选，按词频排序，逐词上屏
LLM 返回后（~0.6s，插到最前）：
│1. 这是一个懒人│2. 这是一个男人│3. 这│4. 这是│…│9. zheshiyigelanren│
     ↑LLM首选      ↑LLM备选(有真歧义才出现)  ↑词库词照旧可选        ↑原文兜底
```

- **词库候选在按键瞬间就有**（本地查询），不用等 LLM——打字手感质变；
- 选词库词=部分上屏：提交该词、吃掉对应音节，剩余拼音继续组合并重新转换；
- 每一个候选要么是 LLM 深思的整句，要么是真实词——不存在"明显不合理"的选项。

## 工作分解

### W1 容错补全 → 拼写变换统一框架（吸收 RIME 拼写运算思想）

- 把现有模糊音/临近键/换位与新增的漏敲（音节挖字反查表：`zhng`→zhang/zheng/zhong）、
  多敲（逐位删字检验）统一为 `SpellingTransform(变换, 可信度)`，一个入口喂 lattice；
- 可信度分级：精确 1.0 > 模糊音 0.9 > 临近键 0.7 > 换位 0.65 > 漏敲/多敲 0.6，
  同时用于候选排序与 prompt 标注措辞（"很可能是/也可能是"）；
- 句尾 partial 音节：`nih` 的 `h` 标注为"未打完，可补全为 hao/hui/…"进 prompt（实时预览质量关键）;
- 被修复的音节在组合区可视标出（借鉴 librime corrector 的知情设计）。

### W2 词库层（本方案核心）

- **数据**：jieba 词频表（MIT 许可，~35 万词）取 top ~5 万 → 离线用
  CFStringTransform 生成词拼音 → 常见多音字补丁表人工校对 → 编译期资源（~1.5MB）。
- **索引**：音节序列前缀 → [词, 频率]，启动建内存 trie（或 mmap 预编译格式，IME 进程冷启 <50ms）。
- **查询**：对切分 lattice 的前缀（含拼写变换路径，按可信度加权）取词候选，频率排序，
  用户词库中的词置顶加权。
- **本地即时造句**（借鉴 RIME）：词库 trie 上 Viterbi（词频 unigram），每次按键毫秒级
  产出一个本地整句作为默认候选；LLM 结果到达且过回验后接管首位。断网可用，感知延迟为零。
- **部分上屏状态机**（借鉴 RIME 逐段确认模型）：composition = 已确认前缀 + 活动段；
  选词即确认段并前进，剩余 buffer 重新切分+转换。这也是 M4 逐段修正复用的段模型。
- **用户词典升级**：tick 衰减加权（旧习惯自然淡出）+ 从整句提交中用词库 trie 切词学习。

### W3 LLM 层收紧（治语义垃圾）

- 备选门槛提高：prompt 明确"仅当存在语义同样通顺的另一种理解时才给第二行，宁缺毋滥"；
- 本地把关：LLM 备选须通过音节一致性回验（CFStringTransform 逐字回拼音，
  容模糊音/修复），且与首选可显示差异 ≥1 字，否则丢弃；
- 首选也过回验：不通过则降为普通候选并把词库首词提为默认——LLM 幻觉不再霸占空格键。

### W4 自绘候选条

横排 NSPanel（数字标注、高亮可控、`-`/`=` 翻页、跟随光标 caret 矩形），
替换 IMKCandidates（根治双轨高亮）。词库候选即时刷新、LLM 结果到达后平滑插入首位。

### W5 评测护栏

- 测试集扩 40 句（补漏敲/多敲/断尾）；指标增加 **top-3 命中率** 与 **词库候选覆盖率**
  （首选错时，正确答案出现在候选栏前 9 项的比例——"没有合适选项"的直接量化）；
- 目标：首选 ≥85%（不回退）、top-3 ≥93%、前 9 项覆盖 ≥98%。

## 详细设计

### D1. 拼写变换统一框架（W1）

```swift
struct SyllableHypothesis {
    var text: String          // 规范音节（如 hao）
    var credibility: Double   // 可信度
    var source: TransformSource
}
enum TransformSource {
    case exact                // 1.0  原样合法
    case fuzzy(String)        // 0.9  模糊音规则（z/zh 等）
    case keyAdjacent          // 0.7  临近键替换（hso→hao）
    case transposition        // 0.65 相邻换位（mne→men）
    case deletion             // 0.6  漏敲（zhng→zhang/zheng/zhong）
    case insertion            // 0.6  多敲（zhoong→zhong）
    case partial([String])    // 0.8  句尾未打完（h→hao/hui/…，仅限 buffer 末尾）
}
```

- **漏敲反查表**（启动时构建，~2500 条）：对每个音节挖掉一个字母生成变体 →
  `deletionMap["zhng"] = [zhang, zheng, zhong]`。用户少敲一个字母时，敲出的串
  正是某音节的"挖字变体"，O(1) 查回。
- **多敲**：对无法解析的串逐位删一个字母（≤6 次尝试），结果合法即为假设。
- **partial 前缀集**：所有音节的所有前缀预生成 Set（~1200 条）；仅当段在 buffer
  末尾时才启用（中间出现的破碎串仍走纠错/透传）。
- 切分 DP 的边代价 = `1.0 - log(credibility)*k`，与现行惩罚制平滑衔接；
  每个音节保留 top-3 假设进 lattice（防组合爆炸）。
- 组合区可视：source != exact 的音节用不同下划线色标出。

### D2. 词库数据管线（W2）

```
scripts/build_lexicon.sh（离线，一次性）:
jieba dict.txt (MIT, 词+词频+词性)
  → 过滤: 纯汉字、长度 2–8 字、频次 top 50k（保留全部高频单字 ~7k）
  → 逐词注音: CFStringTransform(MandarinLatin+StripDiacritics) 逐字
  → 多音字词级补丁: scripts/polyphone_patch.tsv（重庆/银行/音乐… 人工维护，起步 ~100 条）
  → 输出 Resources/lexicon.bin
```

**lexicon.bin 格式**（v1 求简单，不求极致）：
- 音节表：410 音节 → u16 ID
- 词条数组按"音节 ID 序列"字典序排序：`[syllableCount:u8][ids:u16×n][freq:u32][word:utf8]`
- 运行时整体载入内存（~2MB，解析 <100ms），前缀查询 = 二分找区间（排序数组即隐式 trie）

多音字注音错误的后果是"该词用正确拼音查不到"——降低召回但不产生错误候选，
可随补丁表持续修，评测（D6）会暴露头部问题。

### D3. 前缀词候选与本地造句（W2）

```swift
// 从音节位置 i 出发，所有能成词的匹配（含拼写变换假设，按可信度加权）
func matches(lattice: [[SyllableHypothesis]], from i: Int) -> [LexiconMatch]
// LexiconMatch = (word, 消耗音节数, score = log(freq) + Σlog(credibility))
```

**Viterbi 造句**：`dp[j] = max(dp[i] + score(词 i→j) - λ)`，λ 为每词固定惩罚
（偏向长词，起步 λ=4，评测调）。兜底边：单字词；再兜底：音节原文（得分极低，
保证任何输入都有完整路径）。回溯得本地整句 + 每个前缀位置的词候选列表。
复杂度 O(音节数 × 平均词候选数)，n≤20 时 <1ms。

### D4. 候选栏时序与状态机（W2+W4）

每次按键（同步，<5ms）：
```
切分+变换 → 词候选(活动段起点) + Viterbi 本地整句
候选栏 = [本地整句] + [词候选×频序] + [原始拼音]
组合区预览 = 本地整句（点线下划线 = 未定稿）
```
LLM 返回（~0.6s，若 buffer 未变）：
```
回验通过 → 候选栏 = [LLM首选] + [LLM备选?] + [本地整句(若不同)] + [词候选…] + [原文]
           组合区预览 = LLM 首选（实线下划线）
回验失败 → LLM 候选丢弃，本地整句保持首位（点线转实线，标记"本地"）
```

**逐段确认**（选了词候选）：
```
Composition {
  confirmedText: String     // 已确认前缀（仍在 marked text 内，未上屏）
  confirmedKeyCount: Int    // 已消耗的原始按键数（经 Syllable.typed 映射）
  activeBuffer: String      // 剩余按键
}
选词 → confirmedText += 词；按 Syllable.typed 累计长度推进 confirmedKeyCount；
      activeBuffer 剩余部分重新走 D3/D4 全流程（LLM context 追加 confirmedText）
空格 → 上屏 confirmedText + 当前预览；Esc → 整体取消（confirmedText 一并放弃）
退格 → 先退 activeBuffer；activeBuffer 空时回退最近一次确认的词
```

### D5. LLM 收紧与回验（W3）

- prompt：备选"宁缺毋滥"；上下文追加 confirmedText。
- 回验（本地，<5ms）：候选去标点逐字 CFStringTransform 回拼音，与音节假设集比对；
  多音字补充表（~200 常见字）兜底；≥2 字不匹配 → 丢弃，1 字不匹配 → 降权到词候选之后。

### D6. 评测扩展（W5）

- 测试集 40 句：+漏敲×5、+多敲×5、+断尾×5、+需部分上屏的长难句×5；
- 指标：首选命中率 / top-3 / **前 9 项覆盖率**（"没有合适选项"量化）/ 本地整句命中率
  （不开 LLM 时的底线质量）/ p50 延迟；
- 词库自检：期望句中每个词是否可由其拼音在词库中召回（多音字注音错误的探测器）。

## 顺序与依赖

W2（词库）> W1（容错）> W4（候选条）> W3（LLM 收紧）> W5。
W2 是核心也是最大件（数据管线 + trie + 部分上屏状态机）；W1/W3 独立可并行。
简拼、逐段修正仍留 Phase 2 / M4。

## 风险

- CFStringTransform 离线生成词拼音的多音字错误：影响词库质量，需抽样校验 + 补丁表；
  错误后果有限（个别词查不到/排错位），且可持续修补。
- 词库 1.5MB 常驻 + trie 内存 ~10MB：IME 进程可接受。
- 部分上屏后"剩余拼音重转换"会再发一次 LLM 请求：延迟可接受（0.5s），成本略增。

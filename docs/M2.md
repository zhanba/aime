# M2 实施方案：接入 Qwen3-ASR（MLX）

> 目标：ASR 后端协议化，Qwen3-ASR 成为默认本地识别引擎，中英混说质量超过 SpeechAnalyzer 基线；
> 推理下沉到 aime-daemon，XPC 接口定型（为 M3 的 IME 进程做准备）。

## 关键调研结论（2026-07）

| 问题 | 结论 | 对方案的影响 |
|---|---|---|
| 自己移植 MLX 还是用现成库？ | [speech-swift](https://github.com/ivan-digital/qwen3-asr-swift) 已成熟：MLX Swift + CoreML、Qwen3-ASR 0.6B/1.7B（4/5/8-bit）、`transcribeStream()` 流式、自带 VAD、HF 自动下载、Apache 2.0、v0.0.21 活跃维护 | **直接依赖，不移植**。工作量从"移植模型"降为"集成+封装+评测" |
| 开源版支持 context/hotword 偏置？ | **不支持**。官方仓库无相关文档，该能力是 API 版（Qwen3-ASR-Flash）独有 | 热词/用户词库继续走精修层 prompt（M1 已有通道）。保留半天 timebox 试 prompt 隐性注入，不阻塞 |
| 置信度输出？ | 官方未文档化；取决于 speech-swift 是否暴露 token logprobs | 尽力而为项，不设为 M2 出口条件 |
| 时间戳？ | 需配套 Qwen3-ForcedAligner-0.6B 单独模型 | M2 不做，M4 跨模态纠错如需再评估 |

## 工作分解（建议顺序）

### W1 — ASRBackend 协议化（重构 M1，1–2 天）

```swift
protocol ASRBackend: AnyObject {
    var id: ASRBackendID { get }                 // .speechAnalyzer / .qwen3ASR
    func prepare(localeID: String) async throws   // 模型检查/下载/加载
    func makeSession() throws -> ASRSession
}

protocol ASRSession: AnyObject {
    func feed(_ buffer: AVAudioPCMBuffer)         // 音频线程调用
    var updates: AsyncStream<ASRUpdate> { get }   // 增量转写（替换 M1 的 onUpdate 闭包）
    func finish() async throws -> ASRResult
    func cancel() async
}

struct ASRUpdate { var text: String; var isVolatile: Bool }
struct ASRResult {
    var text: String
    var segments: [ASRSegment]?                   // 可选：字级/词级置信度，Qwen 路径暂可为 nil
}
```

- 现有 `TranscriberSession` 改造为 `SpeechAnalyzerBackend`，`AppState` 只面向协议。
- 设置页加「识别引擎」选择器；引擎/模型切换即重建 backend。
- 音频重采样（M1 的 AVAudioConverter 队列逻辑）上提为公共工具，两个后端共用。

### W2 — Qwen3ASRBackend 集成（2–3 天）

- SPM 依赖 speech-swift，**锁精确版本**（0.0.x API 不稳定；所有调用收敛在 `Qwen3ASRBackend.swift` 单文件内，必要时可低成本 fork/vendor）。
- 模型档位：`qwen3-asr-1.7b-4bit`（默认）/ `qwen3-asr-0.6b-4bit`（低配）。
- 模型管理接入设置 UI：下载进度条、磁盘占用、删除；缓存目录统一到 `~/Library/Application Support/aime/models`（或桥接其 `QWEN3_CACHE_DIR`）。
- 国内网络：设置里暴露 HF endpoint 镜像（`HF_ENDPOINT`，如 hf-mirror.com）。
- 常驻策略：首次使用加载进内存，空闲 N 分钟自动卸载（可配置常驻）。

### W3 — VAD 前置 + 会话内流式（2–3 天）

LLM-based ASR 的主要风险是静音/噪声段幻觉，防线在音频入口：

- 用 speech-swift 自带 VAD（Silero 系）替代 M1 的纯 RMS 电平：
  - 掐掉首尾静音，>800ms 的长停顿切段，纯静音会话直接返回空；
  - VAD 概率同时驱动浮层电平动画（比 RMS 更准的"在说话"指示）。
- 流式预览：优先用 `transcribeStream()` 的 partial 结果喂 `ASRUpdate`；不可用则退化为 VAD 分段 + 段内每 ~1s 局部重解码。
- 幻觉后置检测：重复 n-gram 循环检测、文本长度/音频时长比异常 → 丢弃该段并在浮层提示。

### W4 — 置信度（尽力而为，≤1 天）

- speech-swift 若暴露 token logprobs → 按字聚合为 `ASRSegment.confidence`，供浮层低置信标记（M3 组合区虚线体系的数据源）。
- 不暴露则给上游提 issue，M2 出口不依赖此项。

### W5 — daemon 拆分 + XPC 定型（3–4 天）

- 新增 executable target `aime-daemon`，`SMAppService.agent` 注册为 LaunchAgent（bundle 内嵌 plist）。
- 迁移进 daemon：MLX 模型常驻、音频采集（**麦克风权限随之迁移**）、VAD。
- XPC 协议（M3 的 IME 进程复用同一接口）：

```swift
@objc protocol AimeDaemonXPC {
    func prepareModel(backendID: String, reply: @escaping (Error?) -> Void)
    func startSession(config: Data /* codable */, reply: @escaping (String) -> Void) // sessionID
    func finishSession(_ id: String, reply: @escaping (Data?, Error?) -> Void)       // ASRResult
    func cancelSession(_ id: String)
}
@objc protocol AimeDaemonClientXPC {  // 客户端 exported object，接收流式回调
    func sessionUpdate(_ id: String, payload: Data)  // ASRUpdate
    func audioLevel(_ id: String, level: Float)
}
```

- app 侧实现一个 `XPCProxyBackend: ASRBackend`，`AppState` 无感切换。
- 放在最后做：与 Qwen 集成解耦，进度紧张可整体顺延到 M3 而不影响 M2 主目标。

### W6 — 基准评测（2 天，贯穿验收）

- 新增 CLI target：`swift run aime-bench --backend qwen3-1.7b-4bit --dir testdata/`。
- 测试集：50 句中英混说录音 + 人工标注文本（覆盖：纯中文、术语混说、整句英文、专有名词、带停顿长句）。
- 指标：混合 CER/WER（中文按字、英文按词）、RTF、首字延迟、峰值内存（`task_info`）。
- 对比矩阵：SpeechAnalyzer / Qwen3 0.6B / 1.7B × 4-bit/8-bit。

## 验收标准（对应 ROADMAP）

1. 混说测试集上 Qwen3-ASR-1.7B 的错误率低于 SpeechAnalyzer 基线；
2. 目标下限机型（M1 Air 8GB）RTF < 1.0，端到端首字延迟可接受（< 1s）；
3. 引擎在设置中可切换，模型下载/删除可视化；
4. 静音幻觉：10 段纯静音/环境噪声录音，0 条幻觉输出；
5. daemon 拆分完成后，app 重启不重新加载模型（常驻验证）——若 W5 顺延则此条移入 M3。

## 风险与应对

| 风险 | 应对 |
|---|---|
| speech-swift API 不稳定 / 弃维护 | 锁版本 + 单文件封装 + fork 预案；最坏情况参考其实现自写 MLX Swift 推理（Qwen3 解码器在 mlx-swift-examples 有现成实现，音频编码器工作量约一周） |
| 1.7B 在 8GB 机器与 LLM 精修 API 并发时内存吃紧 | 默认档按机器内存自动选择；空闲卸载 |
| HF 下载在国内不可达 | 镜像 endpoint 设置 + 手动导入模型目录入口 |
| 幻觉漏网 | VAD 前置 + 后置启发式双防线；浮层"已丢弃可疑段"提示保持透明 |
